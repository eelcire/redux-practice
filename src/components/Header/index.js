import React from "react";

function Header(props) {
  return (
    <header>
      <main>
        <h1>Todo List</h1>
        <h5>Keep a list of things to do!</h5>
      </main>
    </header>
  );
}

export default Header;
